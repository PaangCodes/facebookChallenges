def censoringWords(censoringWords, sentence):
    sentence = str(sentence)

    if censoringWords in sentence:
        sentence = sentence.replace(censoringWords, "*"*len(censoringWords))

    return sentence


print(censoringWords("for", ["I am here for work"]))
